extend layout

block content
    for item in presentations
        div.card.mb-3.shadow-sm.rounded
            div.card-header
                h5.card-title= item.title + " - " + item.speakerName
            div.card-body!= item.summary
    else
        sorry, no presentations!